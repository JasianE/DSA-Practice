#include <iostream> #include <vector> 
#include <stack> 
using namespace std; 
vector<char> reverseWords( const vector<char>& arr ) { 
  // your code goes here // Queue<string> first in last out // string s = '' // o(n) runtime // O(n) space // ['h', 'e', 'l, ' ', 'e', 'r', 't'] std::stack<string> reverseStack; // Step 1: Add all strings in order to stack string currentString; for(int i{0}; i < arr.size(); i++){ if(arr[i] != ' '){ currentString += arr[i]; // Add character to stirng } else { reverseStack.push(currentString); currentString = ""; } } if(currentString != ""){ reverseStack.push(currentString); } // Step 2: Reverse the order by popping vector<char> reversedWords; while(reverseStack.size() != 0){ string word = reverseStack.top(); for(int i{0}; i < word.size(); i++){ reversedWords.push_back(word[i]); } reversedWords.push_back(' '); reverseStack.pop(); } return reversedWords; // ask before code, this is the approach that im thinking, can i start writing the code? // maybe spend time before coding if there is a better optimal solution, before coding, // just make it clear to the interviewer that you are thinking of other more optimal cases } // comment out main() before running tests int main() { // debug your code below vector<char> arr = {'p', 'e', 'r', 'f', 'e', 'c', 't', ' ', 'm', 'a', 'k', 'e', 's', ' ', 'p', 'r', 'a', 'c', 't', 'i', 'c', 'e'}; vector<char> result = reverseWords(arr); cout << "Reversed words array: "; for (char ch : result) { cout << ch; } cout << endl; return 0; }
